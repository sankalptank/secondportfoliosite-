---
// Main layout component that wraps all pages
// Provides consistent structure, navigation, and styling across the site
import ShaderBackground from '../components/ShaderBackground.astro';
import { ClientRouter } from 'astro:transitions';

interface Props {
	title: string;
}

const { title } = Astro.props;
---

<!doctype html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="description" content="Sankalp Tank - Developer & Designer Portfolio" />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<meta name="generator" content={Astro.generator} />
		<link rel="preconnect" href="https://fonts.googleapis.com">
		<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
		<link href="https://fonts.googleapis.com/css2?family=Kosugi&display=swap" rel="stylesheet">
		<title>{title}</title>
		<ClientRouter />
	</head>
	<body>
		<!-- Shader background canvas -->
		<ShaderBackground />

		<!-- Frame border container -->
		<div class="frame-border">
			<!-- Main wrapper uses flexbox to create full-height layout -->
			<div class="site-wrapper">
				<!-- Navigation component -->
				<nav class="main-nav">
					<!-- Header section with name and title -->
					<header class="nav-header">
						<h1 class="name">Sankalp Tank</h1>
						<p class="subtitle">Developer &<br/>Designer</p>
						<p class="location">NC, USA</p>
					</header>

					<!-- Navigation links -->
					<ul class="nav-links">
						<li><a href="/" class={Astro.url.pathname === "/" ? "active" : ""}>Home</a></li>
						<li><a href="/work" class={Astro.url.pathname === "/work" ? "active" : ""}>Work</a></li>
						<li><a href="/contact" class={Astro.url.pathname === "/contact" ? "active" : ""}>Contact</a></li>
					</ul>
				</nav>

				<!-- Main content area where page content is inserted -->
				<main class="main-content">
					<slot />
				</main>
			</div>
		</div>

		<!-- Dark mask for outside the frame - positioned after frame so it overlays the outer area -->
		<div class="outer-mask"></div>
	</body>
</html>

<style is:global>
	/* Global reset and base styles */
	* {
		margin: 0;
		padding: 0;
		box-sizing: border-box;
		transition: opacity 0.1s ease-in-out;
	}

	body {
		font-family: 'Kosugi', sans-serif;
		background-color: #000;
		color: #fff;
		min-height: 100vh;
		position: relative;
		padding: 2rem;
	}

	/* Frame border container */
	.frame-border {
		position: relative;
		min-height: calc(100vh - 4rem);
		border: 1px solid #fff;
		z-index: 1;
	}

	/* Dark mask for the area outside the frame border */
	.outer-mask {
		position: fixed;
		top: 0;
		left: 0;
		width: 100vw;
		height: 100vh;
		pointer-events: none;
		z-index: 0;
		box-shadow: inset 0 0 0 2rem rgba(0, 0, 0, 0.5);
	}

	/* Main site wrapper using flexbox for layout */
	.site-wrapper {
		display: flex;
		min-height: calc(100vh - 8rem);
		position: relative;
		z-index: 1;
	}

	/* Navigation sidebar styles */
	.main-nav {
		/* Flexbox for vertical layout */
		display: flex;
		flex-direction: column;
		gap: 4rem;
		min-width: 300px;
		padding-left: 2rem;
		padding-top: 2rem;
	}

	/* Header section within nav */
	.nav-header {
		display: flex;
		flex-direction: column;
		gap: 0.5rem;
	}

	.name {
		font-size: 2.5rem;
		font-weight: normal;
		line-height: 1.1;
	}

	.subtitle {
		font-size: 1.25rem;
		font-weight: normal;
		line-height: 1.3;
	}

	.location {
		font-size: 1rem;
		margin-top: 1rem;
	}

	/* Navigation links list */
	.nav-links {
		list-style: none;
		display: flex;
		flex-direction: column;
		gap: 0.5rem;
	}

	.nav-links a {
		color: #fff;
		text-decoration: none;
		font-size: 1.5rem;
		position: relative;
		display: inline-block;
	}

	/* Active link indicator using flexbox pseudo-element */
	.nav-links a.active::after {
		content: '<';
		margin-left: 0.25rem;
	}

	.nav-links a:hover {
		opacity: 0.7;
	}

	/* Main content area */
	.main-content {
		flex: 1;
		display: flex;
		flex-direction: column;
	}

	/* Utility class for centering content */
	.centered-content {
		display: flex;
		flex-direction: column;
		justify-content: center;
		align-items: flex-end;
		height: 100%;
		gap: 2rem;
	}

	/* Section wrapper for work items */
	.work-section {
		display: flex;
		flex-direction: column;
		gap: 3rem;
		justify-content: center;
		flex: 1;
	}

	/* Individual work item using flexbox */
	.work-item {
		display: flex;
		justify-content: space-between;
		align-items: center;
	}

	.work-item h2 {
		font-size: 2rem;
		font-weight: normal;
	}

	.work-item p {
		font-size: 1.5rem;
		text-align: right;
	}

	/* Contact page specific styles */
	.contact-content {
		display: flex;
		height: 100%;
	}

	/* Left side with text content */
	.contact-text {
		flex: 1;
		display: flex;
		align-items: center;
	}

	.contact-text p {
		font-size: 1.5rem;
		line-height: 1.6;
		max-width: 600px;
	}

	/* Right side with social icons */
	.contact-social {
		display: flex;
		align-items: flex-end;
		gap: 2rem;
	}

	.social-icon {
		width: 80px;
		height: 80px;
		background-color: #fff;
		border-radius: 20px;
		display: flex;
		align-items: center;
		justify-content: center;
	}

	.social-icon img {
		width: 50px;
		height: 50px;
	}
</style>
